export const defaultMetaItems = {
  fetching: false,
  fetched: false,
  syncing: false,
  synced: false,
  nextSync: null,
};

export const defaultMetaItem = {
  syncing: false,
  synced: false,
  localId: null,
};
